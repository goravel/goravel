package comdef

// consts for compare operation
const (
	OpEq  = "="
	OpNeq = "!="
	OpLt  = "<"
	OpLte = "<="
	OpGt  = ">"
	OpGte = ">="
)

// consts quote chars
const (
	SingleQuote = '\''
	DoubleQuote = '"'
	SlashQuote  = '\\'

	SingleQuoteStr = "'"
	DoubleQuoteStr = `"`
	SlashQuoteStr  = "\\"
)

// NoIdx invalid index or length
const NoIdx = -1

// const VarPathReg = `(\w[\w-]*(?:\.[\w-]+)*)`
